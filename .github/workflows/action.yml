name: Lotto Buy Bot (로또 구매봇)

on:
  schedule:
    - cron: "50 23 * * 5" # UTC 23:50 = KST 08:50 (토요일 아침)
  workflow_dispatch:

jobs:
  buy-lotto:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        shell: bash
        run: |
          pip install -r requirements.txt
          python -m playwright install chromium --with-deps

      - name: Run Lotto Buy Script
        shell: bash
        run: |
          python ./buy_lotto.py \
            --user-id "${{ secrets.USER_ID }}" \
            --user-pw "${{ secrets.USER_PW }}" \
            --slack-token "${{ secrets.SLACK_BOT_TOKEN }}" \
            --slack-channel "${{ secrets.SLACK_CHANNEL }}" \
            --count "${{ secrets.BUY_COUNT }}" \
            --proxy-user "${{ secrets.PROXY_USER }}" \
            --proxy-pw "${{ secrets.PROXY_PW }}" \
            --proxy-address "${{ secrets.PROXY_ADDRESS }}" \
            --headless \
            --debug \
            --debug-artifacts \
            --timeout-ms 180000

      - name: Upload debug artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: debug-artifacts
          path: artifacts/
          retention-days: 7
